apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base에 있는 기본 뼈대를 그대로 가져옵니다.
resources:
  - ../../../base/questionnaire-service

# 개발 환경만의 특별한 설정
namePrefix: dev- # 모든 리소스 이름 앞에 dev- 를 붙임 (예: dev-board-service)
replicas:
  - name: questionnaire-service
    count: 1 # 파드 1개만 실행